# Normalize to LF in the repository.
* text=auto

# Shell scripts must keep LF even when checked out on Windows, or bash fails on
# the carriage returns. PowerShell and VBScript prefer CRLF.
*.sh    text eol=lf
*.ps1   text eol=crlf
*.vbs   text eol=crlf
